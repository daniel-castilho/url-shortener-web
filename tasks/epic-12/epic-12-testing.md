# Epic 12 – Testing Strategy

    npm test
    npm run test:integration
    npm run check

Optional local:

    npm run build
    # same SBOM + sha256 commands as the workflow

A real tag v* run is Hypothesis until you cut a tag. Cite LOCAL commands in the DoD if no tag.
